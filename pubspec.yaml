name: my_architecture
description: This is my clean architecture

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  auto_route: ^4.2.1
  dio: ^4.0.6
  get_it: ^7.2.0
  equatable: ^2.0.3
  bloc: ^8.0.3
  flutter_bloc: ^8.0.1
  internet_connection_checker: ^0.0.1+4
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  auto_route_generator: ^4.2.0
  build_runner: ^2.2.0

flutter:

  uses-material-design: true
